let a= "Music is the language of the spirit. It opens the secret of life bringing peace.";
//getting index of a word in a string in its first occurance
let firstOccurence=a.indexOf("of");
console.log(firstOccurence);
let lastOccurence=a.lastIndexOf("of");
console.log(lastOccurence);
//slicing a string or cutting a string between two objects
let slicedpart=a.slice(0,30);
console.log(slicedpart);
//the resultant string obtained is called substring
let substring=a.substring(10,20);
console.log(substring);
//slice and substring functions are exactly same in working, the only difference is substring accepts negative values too.
let b="Hii this is MMMUT";
let c=" Gorakhpur";
let con=b+c;
console.log(con);

// Loops in Java script
for(let index=0;index<=10;index++){
    console.log(b);
}

